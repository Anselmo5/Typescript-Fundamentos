function logMessagem(mensagem:string): Array<string> | string {
    console.log("Mesagem do sistema");
    if (mensagem === "anselmo"){
        return [`${mensagem}`]
    }else{
        return "erro"
    }
}

// ^ curiosidade

function djkl(): void {
    console.log(",asndlks");
    
}

djkl();